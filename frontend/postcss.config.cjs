/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

// TODO: rename this file to postcss.config.mjs and use the following export
// export default config;
module.exports = config;
